<?php

namespace App\Domain\{Entity}\Requests;

use Illuminate\Foundation\Http\FormRequest;

class Store{Entity}Request extends FormRequest
{
    /**
     * Determine if the user is authorized to make this request.
     * Note: Primary authorization is handled by the Policy in the controller.
     * This method can add request-level auth checks if needed.
     */
    public function authorize(): bool
    {
        return true;
    }

    /**
     * Get the validation rules that apply to the request.
     *
     * @return array<string, \Illuminate\Contracts\Validation\ValidationRule|array|string>
     */
    public function rules(): array
    {
        return [
            // TODO: Define validation rules
            // 'name'          => ['required', 'string', 'max:255'],
            // 'status'        => ['required', 'in:active,inactive,suspended'],
            // 'category'      => ['required', 'in:defence_plated,contracted_private'],
            // 'document'      => ['sometimes', 'file', 'max:10240', 'mimes:pdf,jpg,png'],
            // 'expiry_date'   => ['sometimes', 'date', 'after:today'],
        ];
    }

    /**
     * Get custom validation messages.
     *
     * @return array<string, string>
     */
    public function messages(): array
    {
        return [
            // 'name.required' => 'The {entity} name is required.',
        ];
    }

    /**
     * Prepare the data for validation.
     * Use this to normalize input before validation runs.
     */
    protected function prepareForValidation(): void
    {
        // Example: normalize plate numbers to uppercase
        // if ($this->has('plate_number')) {
        //     $this->merge(['plate_number' => strtoupper($this->plate_number)]);
        // }
    }
}
